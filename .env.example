# CoinMarketCap API Keys (Required for fallback)
# Supports multiple keys separated by commas for automatic rotation
# Get your free API key at: https://coinmarketcap.com/api/
# Example: COINMARKETCAP_API_KEYS=key1,key2,key3
COINMARKETCAP_API_KEYS=

# Note: Binance API keys are NOT required
# This server uses Binance public endpoints which don't require authentication
