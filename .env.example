# Server Configuration
PORT=3000

# Binance (Optional but recommended for higher limits)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# CoinMarketCap (Required for fallback - Multiple keys supported, comma separated)
# Example: CMC_API_KEYS=key1,key2,key3
CMC_API_KEYS=
